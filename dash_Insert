def DashInsert(num):
	string = str(num)
	result = []
	for charIndex, char in enumerate(string):
	 	result.append(char)
		if int(char) % 2 != 0 and int(string[charIndex + 1]) % 2 != 0:
			result.append("-")
	print ''.join(result)
